name: Skip CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
    - name: Skip CI
      run: |
        echo "Skipping CI checks"
        exit 0